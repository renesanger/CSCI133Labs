#include <iostream>
#define CATCH_CONFIG_MAIN
#include "catch.hpp"
#include "prob.h"
#include "basicmath.h"


/*
//place the catch config above the include.


TEST_CASE("Basic Math Division Test","[divide]"){
	//if you want to see all your tests change require to check , try placing divide(10,2) above everything else
	//require is like check but it stops after the first failure, check looks at them all.REQUIRE(divide(0,3)==0);
	SECTION("this is section 1")
	{
		CHECK(divide(0,3)==0);
		CHECK(divide(10,2)==5);
		CHECK(divide(9,2)==4.5);
	}
	SECTION("this is section 2")
	{
		INFO("this gives you information in your code like a logging facility ")
		for(int i=10; i<20;i++){
			CHECK(divide(i,2)==i/2);
		}
	}
}

//in the terminal when you say ./test -h you get a list of all the commands that you can do.

*/
